services:
  - type: web
    name: daily2odds
    env: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: node build
    envVars:
      - key: NODE_VERSION
        value: 18
      - key: PORT
        value: 3000
    healthCheckPath: /